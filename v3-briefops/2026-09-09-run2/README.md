# U1 BriefOps v3 — 2026-09-09 Run 2

Safe delta overlay against U1 Sentinel v3 / AuthorityBench v3.

This overlay does not execute external effects. It preserves the U1 invariant that model capability, supplier, funding, hardware, or external scanner results cannot widen principal authority.

Run:

```bash
python -m pytest -q
```
